# Print and PDF

To print or export this documentation:

- Use your browser's Print dialog (Ctrl+P / Cmd+P)
- For a PDF, choose "Save as PDF" as the printer

Tip: For a clean print, hide the navigation using the sidebar toggle.

---

Metadata

```yaml
status: "available"
last_updated: "{{ now().strftime('%Y-%m-%d') }}"
```
